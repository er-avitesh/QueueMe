spring:
  datasource:
    platform: h2
    url: jdbc:h2:mem:merchantDB
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console